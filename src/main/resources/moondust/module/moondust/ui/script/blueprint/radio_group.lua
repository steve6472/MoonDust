function init(input)
    return {
        tables = {
            ["moondust:radio_group"] = {
                group = input.group,
                selected = input.selected,
                label = input.label
            }
        }
    }
end
