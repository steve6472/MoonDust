{
    fields: {
        test: {
            // Allow recursion, fuck, don't use "table" probably
            type: "table",
            fields: {
                other_test: {
                    type: "int"
                }
            }
        }
    }
}