function init(input)
    return {
        tables = {
            ["mcfont:text_area"] = {
                group = input.group,
                selected = input.selected,
                label = input.label
            }
        }
    }
end
